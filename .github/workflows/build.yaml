name: Dependency Vulnerability Scan with Snyk

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  snyk-scan:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Python (if using Python project)
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Set up Node.js (if using Node.js project)
        uses: actions/setup-node@v2
        with:
          node-version: '14'  # Cambia según la versión de tu proyecto

      - name: Install dependencies
        run: |
          if [ -f package.json ]; then
            npm install
          elif [ -f requirements.txt ]; then
            pip install -r requirements.txt
          fi

      - name: Install Snyk
        run: |
          npm install -g snyk  # Para Node.js
          # pip install snyk  # Para Python, si es necesario

      - name: Run Snyk test
        run: snyk test

      - name: Monitor project with Snyk (optional)
        run: snyk monitor
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}  # Asegúrate de configurar tu token de Snyk en los secretos del repositorio
